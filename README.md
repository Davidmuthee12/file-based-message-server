# File-Based Message Server (Node.js)

A basic HTTP server built with Node.js core modules that allows messages to be written to a file and read back through simple HTTP routes.  
This project demonstrates asynchronous file handling and server-side logic without using any external libraries.

---

## 🚀 Features
- Write messages to a file via an HTTP route
- Read stored messages from a file
- Uses asynchronous file system operations
- Handles invalid routes with proper HTTP status codes

---

## 🧠 Concepts Demonstrated
- Node.js core modules:
  - `http`
  - `fs`
  - `path`
- Creating an HTTP server
- Basic routing
- Asynchronous file read/write operations
- Server response handling

---

## 📂 Project Structure
│
├── index.js
├── output.txt
└── README.md


---

##  Usage

### Start the server
```bash
node index.js


